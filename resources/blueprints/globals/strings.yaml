tabs:
  main:
    display: Main
    sections:
      -
        display: Strings
        instructions: 'Strings that still need to be updated in CP but make more sense to be set globally instead of inside a Block or every Entry'
        fields:
          -
            handle: related_posts
            field:
              display: 'Related posts'
              type: text
